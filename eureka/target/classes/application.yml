spring:
  application:
    name: eureka
server:
  port: 8761
eureka:
  client:
    fetch-registry: false #不从eureka上获取服务
    register-with-eureka: false #不把自己注册到eureka上
    service-url:
      defaultZone: http://localhost:8761/eureka/